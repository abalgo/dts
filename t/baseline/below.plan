#!/bin/sh
# generated by mutationstructure.pl
#   source      : FIX/below/work/src.dts   (root 'FIX/below/src')
#   destination : FIX/below/work/dst.dts   (root 'FIX/below/work/dst')
#   filter      : (none)   min size : 100
# REVIEW BEFORE RUNNING.

set -e
cd 'FIX/below/work/dst' || exit 1

mkdir -p 'Other'
mkdir -p 'Target'


# --- files ---
mv -n 'Mixed/p3.txt' 'Other/p3.txt'
mv -n 'Mixed/p4.txt' 'Other/p4.txt'
mv -n 'Mixed/p1.txt' 'Target/p1.txt'
mv -n 'Mixed/p2.txt' 'Target/p2.txt'

# --- cleanup (harmlessly fails if not empty) ---
rmdir 'Mixed' 2>/dev/null || true
