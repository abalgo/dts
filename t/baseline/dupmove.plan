#!/bin/sh
# generated by mutationstructure.pl
#   source      : FIX/dupmove/work/src.dts   (root 'FIX/dupmove/src')
#   destination : FIX/dupmove/work/dst.dts   (root 'FIX/dupmove/work/dst')
#   filter      : (none)   min size : 100
# REVIEW BEFORE RUNNING.

set -e
cd 'FIX/dupmove/work/dst' || exit 1

mkdir -p 'Photos'


# --- files ---
mv -n 'Old/a.jpg' 'Photos/a.jpg'
mv -n 'Old/b.jpg' 'Photos/b.jpg'
mv -n 'Old/c.jpg' 'Photos/c.jpg'
mv -n 'Old/d.jpg' 'Photos/d.jpg'

# --- duplicates (1) ---
rm -f 'Old/dup/a.jpg'

# --- cleanup (harmlessly fails if not empty) ---
rmdir 'Old/dup' 2>/dev/null || true
rmdir 'Old' 2>/dev/null || true
