#!/bin/sh
# generated by mutationstructure.pl
#   source      : FIX/nested/work/src.dts   (root 'FIX/nested/src')
#   destination : FIX/nested/work/dst.dts   (root 'FIX/nested/work/dst')
#   filter      : (none)   min size : 100
# REVIEW BEFORE RUNNING.

set -e
cd 'FIX/nested/work/dst' || exit 1



# --- files ---
mv -n 'Android/media/clip1.mp3' 'Android/clip1.mp3'
mv -n 'Android/media/clip2.mp3' 'Android/clip2.mp3'
mv -n 'Android/media/clip3.mp3' 'Android/clip3.mp3'
mv -n 'Android/media/clip4.mp3' 'Android/clip4.mp3'
mv -n 'Android/media/clip5.mp3' 'Android/clip5.mp3'

# --- cleanup (harmlessly fails if not empty) ---
rmdir 'Android/media' 2>/dev/null || true
