#!/bin/sh
# generated by mutationstructure.pl
#   source      : FIX/subrename/work/src.dts   (root 'FIX/subrename/src')
#   destination : FIX/subrename/work/dst.dts   (root 'FIX/subrename/work/dst')
#   filter      : (none)   min size : 100
# REVIEW BEFORE RUNNING.

set -e
cd 'FIX/subrename/work/dst' || exit 1


# --- directories ---
mv -n 'Album/OldSub' 'Album/Sub'

# --- cleanup (harmlessly fails if not empty) ---
rmdir 'Album' 2>/dev/null || true
