#!/bin/sh
# generated by mutationstructure.pl
#   source      : FIX/fuzzyswap/work/src.dts   (root 'FIX/fuzzyswap/src')
#   destination : FIX/fuzzyswap/work/dst.dts   (root 'FIX/fuzzyswap/work/dst')
#   filter      : (none)   min size : 100
# REVIEW BEFORE RUNNING.

set -e
cd 'FIX/fuzzyswap/work/dst' || exit 1

mkdir -p 'New'


# --- files ---
mv -n 'Old/y.txt' 'New/x.txt'
mv -n 'Old/x.txt' 'New/y.txt'
mv -n 'Old/z.txt' 'New/z.txt'

# --- cleanup (harmlessly fails if not empty) ---
rmdir 'Old' 2>/dev/null || true
