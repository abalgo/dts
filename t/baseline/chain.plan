#!/bin/sh
# generated by mutationstructure.pl
#   source      : FIX/chain/work/src.dts   (root 'FIX/chain/src')
#   destination : FIX/chain/work/dst.dts   (root 'FIX/chain/work/dst')
#   filter      : (none)   min size : 100
# REVIEW BEFORE RUNNING.

set -e
cd 'FIX/chain/work/dst' || exit 1

mkdir -p 'Chain/A/B'


# --- files ---
mv -n 'Chain/P/Q/two.txt' 'Chain/A/B/one.txt'

# --- cleanup (harmlessly fails if not empty) ---
rmdir 'Chain/P/Q' 2>/dev/null || true
